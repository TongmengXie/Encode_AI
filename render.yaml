services:
  - type: web
    name: wandermatch
    runtime: python
    buildCommand: poetry install
    startCommand: poetry run uvicorn wander_match.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.9"